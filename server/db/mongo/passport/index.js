import deserializeUser from './deserializeUser';
import google from './google';
import local from './local';
import github from './github';

export { deserializeUser, google, github, local };

export default {
  deserializeUser,
  google,
  github,
  local,
};
